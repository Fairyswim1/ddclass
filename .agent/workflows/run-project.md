---
description: 서버와 클라이언트를 자동으로 구동합니다.
---

이 워크플로우는 백엔드 서버와 프런트엔드 클라이언트를 동시에 실행합니다.

// turbo
1. 백엔드 서버 실행
```powershell
cd server; npm install; node index.js
```

// turbo
2. 프런트엔드 클라이언트 실행
```powershell
cd client; npm install; npm run dev
```
